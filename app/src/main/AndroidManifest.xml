<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="in.ckd.calenderkhanado"
    android:versionCode="5"
    android:versionName="1.4.0">

    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="in.ckd.calenderkhanado.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name="in.ckd.calenderkhanado.activities.SpashAcctivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="in.ckd.calenderkhanado.activities.LoginAndSignupActivity" />
        <activity android:name="in.ckd.calenderkhanado.activities.InterestActivity" />
        <activity android:name="in.ckd.calenderkhanado.activities.RateYourselfActivity" />
        <activity android:name="in.ckd.calenderkhanado.location.UserLocationActivtiy" />
        <activity android:name="in.ckd.calenderkhanado.activities.ChooseFoodTypeActivity" />
        <activity android:name="in.ckd.calenderkhanado.activities.MainBottomBarActivity" />
        <activity android:name="in.ckd.calenderkhanado.fragments.OrderDetailsActivity" />
        <activity android:name="in.ckd.calenderkhanado.activities.ActiveOrderActivtiy" />
        <activity android:name="in.ckd.calenderkhanado.fragments.ProfileFragmentChef" />
        <activity android:name="in.ckd.calenderkhanado.activities.MobileOtpVerificationActivity" />
        <activity android:name="in.ckd.calenderkhanado.location.EnterFullAdressActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAiLBFmkx2Q0T7JwBUERsv6H8JH03ZGpJc" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name="in.ckd.calenderkhanado.activities.ProfileUpdateActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- start firebase push notification -->

        <service android:name="in.ckd.calenderkhanado.service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name="in.ckd.calenderkhanado.service.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <service android:name="in.ckd.calenderkhanado.Location.GpsTracker"/>

        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/spwhite" />
        <!--
             Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
        <!-- end firebase push notification -->

        <activity android:name="in.ckd.calenderkhanado.activities.YourCartActivity" />
        <activity android:name="in.ckd.calenderkhanado.activities.CongratualtionsActivtiy" />
        <activity
            android:name="in.ckd.calenderkhanado.activities.ManageAddressesActivity"
            android:label="@string/title_activity_manage_addresses"
            android:theme="@style/AppTheme" />
        <activity android:name="in.ckd.calenderkhanado.activities.ManagePaymentActivity" />
        <activity android:name="in.ckd.calenderkhanado.activities.HelpSupportActivity"/>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.DarkActionBar" />

    </application>

</manifest>