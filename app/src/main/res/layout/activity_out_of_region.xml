<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.OutOfRegion">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginTop="120dp"
        android:layout_centerHorizontal="true"
        android:src="@mipmap/ckd_ic_launcher"/>

   <com.mf.mumizzfood.widgets.CkdTextview
       android:id="@+id/outofInderText"
       android:layout_width="wrap_content"
       android:layout_height="match_parent"
       android:layout_margin="@dimen/dp24"
       android:layout_below="@+id/logo"

       style="@style/textviewbold"
       android:textSize="20sp"
       android:text="Thank you for considering us.we are not in your city right now.
we will be there very Soon.

Team
MumizzFood"/>
</RelativeLayout>